meta {
  name: [에러] 잘못된 status 값 → 400
  type: http
  seq: 11
}

patch {
  url: {{baseUrl}}/api/schedule/item/{{itemId}}
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "status": "wrong_status"
  }
}

tests {
  test("400 반환", function() {
    expect(res.status).to.equal(400);
  });
}
